# Fill these in via cPanel "Setup Node.js App" Environment Variables,
# or create a .env file in the app root on the server.
DATABASE_URL=
PORT=3001
NODE_ENV=production
APP_URL=https://productoramda.cl
FRONTEND_URL=https://productoramda.cl
SMTP_USER=
SMTP_PASS=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
